import './ReactToThis.css'

const ReactToThis = () => (
	<div>
		<h1> React to this </h1>
	    <p> <span className="cursive">This is a React app</span>, consisting of a single component</p>

	    <p>Instead of HTML, it's a <span className="underline">function that returns JSX</span>.</p>
	</div>
)

export default ReactToThis